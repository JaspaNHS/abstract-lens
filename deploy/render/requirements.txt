# Runtime dependencies for serving Abstract Lens on Render (free tier).
# chromadb is PINNED so the prebuilt index is read by the same version that built it.
chromadb==1.5.9
onnxruntime==1.26.0
flask==3.1.3
anthropic==0.109.1
gunicorn==23.0.0
