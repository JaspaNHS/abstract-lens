# Runtime-only dependencies for serving Abstract Lens on Hugging Face Spaces.
# (No scraping/download/processing libs needed — the index is prebuilt and shipped.)
# chromadb is PINNED: the vector index must be read by the same version that built it.
chromadb==1.5.9
onnxruntime==1.26.0
flask==3.1.3
anthropic==0.109.1
gunicorn==23.0.0
